/**
 * AxisEditor re-export from @variscout/ui
 *
 * Uses PWA semantic tokens (default color scheme).
 * For new code, import directly from '@variscout/ui'.
 */
export { AxisEditor as default, type AxisEditorProps } from '@variscout/ui';
