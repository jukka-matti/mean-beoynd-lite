/**
 * ErrorBoundary re-export from @variscout/ui
 *
 * Uses PWA semantic tokens (default color scheme).
 * For new code, import directly from '@variscout/ui'.
 */
export { ErrorBoundary as default, type ErrorBoundaryProps } from '@variscout/ui';
