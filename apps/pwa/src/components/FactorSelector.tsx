/**
 * FactorSelector re-export from @variscout/ui
 *
 * Uses PWA semantic tokens (default color scheme).
 * For new code, import directly from '@variscout/ui'.
 */
export { FactorSelector as default, type FactorSelectorProps } from '@variscout/ui';
