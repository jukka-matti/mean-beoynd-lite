/**
 * PWA WhatIfSimulator — re-exports from @variscout/ui with default (PWA) color scheme.
 */
export {
  WhatIfSimulator as default,
  type WhatIfSimulatorProps,
  type SimulatorPreset,
  type WhatIfSimulatorHandle,
} from '@variscout/ui';
